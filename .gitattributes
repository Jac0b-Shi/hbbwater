# Keep source and container files stable across Windows and Linux checkouts.
* text=auto eol=lf

# PowerShell scripts are maintained with Windows line endings.
*.ps1 text eol=crlf
*.psm1 text eol=crlf
